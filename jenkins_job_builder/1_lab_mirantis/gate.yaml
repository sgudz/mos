- job:
    name: gate
    description: 'Gate job for mos-scale project'
    display-name: 'Gate job for gerrit'
    node: master
    logrotate:
      daysToKeep: 62
      numToKeep: 100
    wrappers:
        - timestamps
    scm:
        - main-git-gerrit
    triggers:
        - gerrit:
            trigger-on:
                - patchset-created-event
            projects:
                - project-compare-type: 'PLAIN'
                  project-pattern: 'mos-scale/mos-scale'
                  branches:
                    - branch-compare-type: ANT
                      branch-pattern: '**'
            server-name: gerrit.mirantis.com
    builders:
        - shell:
             !include-raw-escape scripts/gate.sh
    publishers:
        - junit:
            results: '*.xml'